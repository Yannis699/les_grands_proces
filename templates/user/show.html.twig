{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-light">
        <h2 class=""> {{ user.fullname }} </h2>
        <div class="text-light">
        <img src="{{ user.picture}}">
            <ul class="">
                <li class="mb-2 text-light"><strong>Prénom : </strong>{{ user.firstname }}</li>
                <li class="mb-2 text-light"><strong> Nom : </strong>{{ user.lastname }}</li>
                <li class="mb-2 text-light"><strong> Email : </strong>{{user.email}}</li>
            </ul>
        </div>
    </div>
{% endblock %}